#ifndef COLOR_H
#define COLOR_H

/// <summary>
/// A color is a generic non-directx dependent value used to communicate to the renderer.
/// </summary>
enum Color
{
	BLACK,
	WHITE,
	GRAY,
	RED,
	GREEN,
	BLUE
};

#endif